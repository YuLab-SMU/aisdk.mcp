% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_client.R
\name{create_mcp_client}
\alias{create_mcp_client}
\title{Create an MCP Client}
\usage{
create_mcp_client(
  command,
  args = character(),
  env = NULL,
  sampling_handler = NULL
)
}
\arguments{
\item{command}{The command to run the MCP server}

\item{args}{Command arguments}

\item{env}{Environment variables}

\item{sampling_handler}{Optional \verb{function(params)} answering server
\code{sampling/createMessage} requests; see \code{\link[=mcp_sampling_handler]{mcp_sampling_handler()}}. When set,
the client advertises the \code{sampling} capability.}
}
\value{
An McpClient object
}
\description{
Convenience function to create and connect to an MCP server.
}
\examples{
\donttest{
if (interactive()) {
  # Connect to GitHub MCP server
  client <- create_mcp_client(
    "npx",
    c("-y", "@modelcontextprotocol/server-github"),
    env = c(GITHUB_PERSONAL_ACCESS_TOKEN = Sys.getenv("GITHUB_TOKEN"))
  )

  # List available tools
  tools <- client$list_tools()

  # Use tools with generate_text
  result <- generate_text(
    model = "openai:gpt-4o",
    prompt = "List my GitHub repos",
    tools = client$as_sdk_tools()
  )

  client$close()
}
}
}
