% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_sampling.R
\name{mcp_sampling_handler}
\alias{mcp_sampling_handler}
\title{Create an aisdk-backed sampling handler}
\usage{
mcp_sampling_handler(model = NULL, ...)
}
\arguments{
\item{model}{An aisdk model object or id string used to answer sampling
requests (e.g. \code{"anthropic:claude-sonnet-5"}).}

\item{...}{Extra arguments forwarded to \code{\link[aisdk:generate_text]{aisdk::generate_text()}} on every
sampling call (e.g. \code{temperature}).}
}
\value{
A function of one argument (the request params).
}
\description{
Returns a \verb{function(params)} suitable as an \link{McpClient}'s \code{sampling_handler}:
it converts an incoming \code{sampling/createMessage} request into an aisdk
\code{\link[aisdk:generate_text]{aisdk::generate_text()}} call and formats the reply as an MCP
\code{CreateMessageResult}. The server's \code{modelPreferences} are advisory — the
\code{model} you pass here is authoritative, which keeps API keys on the client.
}
