% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_sampling.R
\name{mcp_handle_server_request}
\alias{mcp_handle_server_request}
\title{Dispatch a server-initiated request to the right client handler}
\usage{
mcp_handle_server_request(msg, sampling_handler = NULL)
}
\arguments{
\item{msg}{The deserialized JSON-RPC request from the server.}

\item{sampling_handler}{Optional \verb{function(params)} returning a
CreateMessageResult; \code{NULL} if the client did not register one.}
}
\value{
A JSON-RPC response list (never signals; handler errors become a
JSON-RPC internal error so the server always gets a reply).
}
\description{
A client must answer certain server->client requests. This turns one such
request into the JSON-RPC response to send back: \code{ping} (liveness),
\code{sampling/createMessage} (routed to \code{sampling_handler}), and a
method-not-found error for anything else.
}
\keyword{internal}
