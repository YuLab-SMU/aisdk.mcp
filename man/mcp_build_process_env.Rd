% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_env.R
\name{mcp_build_process_env}
\alias{mcp_build_process_env}
\title{Build the environment for a spawned MCP server process}
\usage{
mcp_build_process_env(env = NULL, inherit_env = FALSE)
}
\arguments{
\item{env}{A named character vector of variables to pass (the caller's
deliberate choice, e.g. the server's own token). \code{NULL} for none.}

\item{inherit_env}{Logical. \code{FALSE} (default, secure) passes only the safe
allowlist; \code{TRUE} passes the parent environment minus secrets.}
}
\value{
A named character vector suitable for \code{processx}'s \code{env}.
}
\description{
By default (\code{inherit_env = FALSE}) the child gets only a minimal allowlist of
non-secret system variables plus whatever the caller passes in \code{env} — so no
API key reaches the server unless it was shared explicitly. With
\code{inherit_env = TRUE} the child inherits the parent environment but with
recognised credentials stripped out (defence in depth). The caller's explicit
\code{env} always wins.
}
\keyword{internal}
